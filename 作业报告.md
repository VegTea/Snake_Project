# Snake_Project 作业报告

做了很多尝试，最终提交版本为非端到端的rl版本

### 端到端RL

域随机化部分只修改了摩擦因子的部分

尝试了很多次reward的设计，参数的调整，有两种不同的思路，第一种是直接优化目标函数planar_MAE，把这个error加入到reward里面weight * exp(-err^2 / std) 的形式，eval结果为planar_MAE = 0.1218，但是蛇无法学会规则步态，在原地“蠕动”，速度基本为0

第二种思路是先让蛇学会像正弦横波一样前进，然后再做速度跟踪，设置了一个课程学习，中间切换阶段的时候reward权重线性变化，如下所示

```py
reward_weights = CurrTerm(
    func=mdp.reward_weight_stage_curriculum,
    params={
        "gait_iterations": 1000,
        "transition_iterations": 1000,
        "steps_per_iteration": 24,
        "gait_weights": {
            "track_lin_vel_xy_exp": 0.0,
            "sine_wave_position": 5.0,
            "sine_wave_velocity": 0.5,
            "phase_propagation": 0.5,
            "joint_amplitude": 0.3,
        },
        "velocity_weights": {
            "track_lin_vel_xy_exp": 5.0,
            "sine_wave_position": 0.0,
            "sine_wave_velocity": 0.0,
            "phase_propagation": 1.0,
            "joint_amplitude": 0.3,
        },
    },
```

但这个方法如果速度追踪训练步数长的话蛇还是会收敛到第一种方法中的步态，因此速度跟踪阶段没有设置的特别长，也是1000 iterations，平衡步态与性能之后课程学习的结果是planar_MAE=0.2963，蛇形机器人学会了蜿蜒前行和转弯，但是速度追踪效果还是不好

### 解析步态+PD控制

根据观察，蛇形机器人有7个纵向的关节电机，并且每一截身体长度相同，这里人为设定蛇的gait为一个周期的正弦波向前传播的形式，那么七个点在一个周期内，在首尾相位相同的情况下相邻关节的相位差是 $\frac{\pi}{3}$，该gait下电机(从头到尾编号0,1,...,6)轨迹可以表示为：

$$
q_i(t)=A\sin(2\pi ft + i \Delta\phi)+b
$$

>A是振幅，f是频率，b是偏置；
$\Delta\phi = \pi / 3$ 人为设定

当b为0的时候A和f共同决定蛇前进的速度，这里固定A=0.2，于是可以通过控制f来控制蛇前进速度

b不为0时，所有关节多一个偏置，蛇身体会在正弦波的基础上弯曲一个角度，所以可以通过控制b来控制蛇身体弯曲程度来完成转弯的效果

在这个解析步态的基础上，我尝试使用一个简单的PD控制器来调节 $f$ 和 $b$。$f$ 控制速度 $b$ 控制方向。

由于 $b$ 直接影响蛇身体弯曲程度，如果变化太快会导致蛇突然大幅转弯，所以实际控制时没有直接把 $b$ 设置为 $b_{target}$，而是加入了变化率限制：

$$
b \leftarrow b + clip(b_{target}-b, -\Delta b_{max}, \Delta b_{max})
$$

这样可以让转弯更加平滑。简单来说，这个PD控制器的作用是：

- 速度慢了就增大频率 $f$，速度快了就减小频率 $f$；
- 方向偏了就调整偏置 $b$，让蛇身体向目标方向弯曲；
- 对 $b$ 做限速，避免转弯过于剧烈。

这个方法的效果是最好的，评测的planar_MAE达到了**0.0675**，非常优美，轨迹图可以看附件`eval_output_sin_controller.zip`

这个方法的优点是原理清楚，可解释性强不需要训练就能产生基本的前进和转弯行为。但是**缺点是依赖Virtual Chassis速度的反馈**。如果最终策略只能接收有限的本体观测（比如只能接收到电机有关信息），就不能直接使用完整的PD反馈控制做速度追踪。

### RL输出步态参数（最终提交）

把policy的输出改成两个维度`[f, b]`，然后用方法二中的步态公式计算电机的轨迹来进行控制

reward设计和端到端RL中的速度追踪部分一致，仅仅修改weight参数：

```py
track_lin_vel_xy_exp = RewTerm(
    func=mdp.VirtualChassisTrackLinVelXYExp,
    weight=4.0,
    params={"command_name": "base_velocity", "std": 0.1, "linear_coef": 0.1, "asset_cfg": virtual_chassis_body_cfg()},
)

track_ang_vel_z_exp = RewTerm(
    func=mdp.VirtualChassisTrackAngVelZExp,
    weight=4.0,
    params={"command_name": "base_velocity", "std": 0.1, "linear_coef": 0.1, "asset_cfg": virtual_chassis_body_cfg()},
)
```