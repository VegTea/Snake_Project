# Snake_Project 仓库架构

本仓库基于 **IsaacLab + RSL-RL** 构建，使用深度强化学习（PPO）训练 **14DOF-DW 蛇形机器人**完成速度跟踪任务。核心理念是通过 **Virtual Chassis（虚拟底盘）** 将蛇形机器人扭动的身体抽象为一个平稳参考系，在其上进行速度指令跟踪。

---

## 目录结构总览

```
Snake_Project/
│
├── 📁 scripts/                                   # ===== 脚本入口层 =====
│   ├── rsl_rl/
│   │   ├── train.py                              # 【训练主脚本】启动IsaacSim，用RSL-RL的PPO算法并行训练策略
│   │   │                                         # 支持多GPU分布式、视频录制、checkpoint恢复，输出jit/onnx策略
│   │   ├── play.py                               # 【推理回放脚本】加载训练好的策略在IsaacSim中推演
│   │   │                                         # 支持手动速度指令(--cmd_vx/vy/wz)、视频录制、导出jit/onnx、
│   │   │                                         # 可视化Virtual Chassis轨迹/速度跟踪曲线
│   │   └── cli_args.py                           # 【CLI参数工具】定义统一命令行参数(add_rsl_rl_args)和
│   │                                             # 配置更新逻辑(update_rsl_rl_cfg)
│   ├── zero_agent.py                             # 【零动作基准】输出全零动作，用于测试环境是否正常运行
│   ├── random_agent.py                           # 【随机动作基准】输出随机动作(-1到1)，用于验证环境交互
│   └── list_envs.py                              # 【环境枚举】打印所有已注册的Snake-* Gym环境名称和配置路径
│
├── 📁 sim2sim/                                   # ===== Sim2Sim迁移评估层 =====
│   ├── sim2sim_mujoco.py                         # 【单指令MuJoCo运行器】将IsaacLab训练的策略(.pt jit)加载到
│   │                                             # MuJoCo物理引擎中运行，支持固定速度指令、实时/加速推演、
│   │                                             # 输出Virtual Chassis和base_link的速度/轨迹可视化图
│   ├── sim2sim_eval.py                           # 【批量评估脚本】在5×5速度指令网格(vx×vy)上自动运行25组
│   │                                             # 实验，计算MAE误差，生成热力图/轨迹汇总图/速度跟踪曲线
│   └── utils/
│       └── virtual_chassis.py                    # 【Virtual Chassis工具类】SVD分解计算虚拟底盘位姿/速度，
│                                                 # 封装轨迹、速度的可视化绘图与npz数据导出
│
├── 📁 source/snake_project/                      # ===== Isaac Lab扩展包(核心) =====
│   ├── pyproject.toml / setup.py                 # 【包管理】pip install -e 安装配置
│   │
│   └── snake_project/
│       ├── __init__.py                           # 【入口】注册Gym环境、UI扩展，定义SNAKE_ROOT_DIR路径
│       ├── ui_extension_example.py               # 【UI扩展示例】IsaacSim界面扩展模板
│       │
│       ├── 📁 assets/Snake/
│       │   └── __init__.py                       # 【机器人资产定义】配置14DOF-DW的USD模型路径、
│       │                                         # 关节属性(阻尼/刚度/力限制)、两类执行器(body_joints为
│       │                                         # yaw/pitch主动关节，wheel_joints为被动轮零力矩)
│       │
│       ├── 📁 tasks/manager_based/velocity_tracking/
│       │   ├── velocity_env_cfg.py               # 【环境总配置】组装场景(平面地形+机器人+光照+接触传感器)
│       │   │                                     # +指令+观测+动作+奖励+事件+终止+课程，定义关节名/VC体名
│       │   │
│       │   ├── 📁 mdp/                           # -- MDP核心组件 --
│       │   │   ├── virtual_chassis.py            # 【VC数学核心】SVD分解计算Virtual Chassis参考系：
│       │   │   │                                 # compute_virtual_chassis_frame()计算几何中心+主轴方向
│       │   │   │                                 # compute_virtual_chassis_command_terms()计算VC系线/角速度
│       │   │   │                                 # 含坐标轴符号一致性处理(防翻转)和右手系保证
│       │   │   │
│       │   │   ├── commands.py                   # 【速度指令系统】VirtualChassisVelocityCommand类：
│       │   │   │                                 # 在VC参考系中生成/更新线速度和角速度指令，管理指令重采样
│       │   │   │                                 # 支持heading模式、课程学习的范围扩展/收缩、可视化调试标记
│       │   │   │
│       │   │   ├── rewards.py                    # 【奖励函数集】共10个奖励项：
│       │   │   │                                 # - VirtualChassisTrackLinVelXYExp: VC系线速度跟踪(指数+线性惩罚)
│       │   │   │                                 # - VirtualChassisTrackAngVelZExp: VC系角速度跟踪
│       │   │   │                                 # - joint_amplitude: 鼓励持续运动(平均关节速度)
│       │   │   │                                 # - phase_propagation: 相邻关节运动相位传播(交替速度方向)
│       │   │   │                                 # - motion_coordination: 惩罚全关节同向运动
│       │   │   │                                 # - RawActionRatePenalty: 动作平滑(L2一阶差分)
│       │   │   │                                 # - contact_penalty: 自接触惩罚(检测身体碰撞)
│       │   │   │                                 # - ang_vel_xy_l2/joint_torques_l2/joint_acc_l2: 正则化项
│       │   │   │                                 # - is_terminated: 终止惩罚
│       │   │   │
│       │   │   ├── observations.py               # 【观测函数集】6个观测项(总30维)：
│       │   │   │                                 # base_ang_vel(3)+projected_gravity(3)+velocity_commands(3)
│       │   │   │                                 # +joint_pos_rel(7)+joint_vel_rel(7)+last_actions(7)
│       │   │   │
│       │   │   ├── events.py                     # 【事件】reset_snake_state(): 随机初始化机器人root位姿/
│       │   │   │                                 # 速度/关节角度(含注释掉的域随机化：质量/质心/执行器增益)
│       │   │   │
│       │   │   ├── terminations.py               # 【终止条件】invalid_state(): 检测高度越界/线速度超限/
│       │   │   │                                 # 角速度超限/NaN值，触发episode终止
│       │   │   │
│       │   │   └── curriculums.py                # 【课程学习】command_velocity_curriculum():
│       │   │                                     # 基于EMA平滑的跟踪奖励自适应扩展/收缩速度指令范围
│       │   │
│       │   └── 📁 config/Snake_7DOF/
│       │       ├── flat_env_cfg.py               # 【平坦地形具体配置】继承基础配置，设平面地形
│       │       │                                 # 含PLAY变体: 单环境、禁观测噪声、更大速度范围
│       │       └── agents/
│       │           └── rsl_rl_ppo_cfg.py         # 【PPO超参数】网络结构[512,256,128]×2(actor/critic)
│       │                                         # 算法: clip_param=0.2, γ=0.99, λ=0.95, lr=1e-3,
│       │                                         # entropy_coef=0.01, 自适应KL调度
│       │
│       └── 📁 data/Snake/                        # -- 机器人模型数据 --
│           ├── 14DOF-DW.xml                      # MuJoCo MJCF模型文件(sim2sim用)
│           ├── urdf/                             # 14DOF-DW的USD/URDF模型+物理/传感器配置
│           ├── meshes/                           # 各link的STL网格(link1-14, base_link, 左右轮等)
│           ├── config/                           # 关节名称映射YAML
│           └── launch/                           # Gazebo/display ROS launch文件
│
└── 📁 fig/                                       # ===== 文档图片 =====
    ├── fig1.jpg  (机器人实物/mockup)
    ├── fig2.gif  (运动步态动画)
    ├── fig3.png  (Base_link vs VC 轨迹对比)
    ├── fig4.png  (Base_link vs VC 速度对比)
    ├── fig5.jpg  (服务器安装截图)
    └── fig6.jpg  (eval_output 输出示例)
```

---

## 数据流架构

```
┌──────────────────────────────────────────────────────────────────────┐
│                          train.py / play.py                          │
│                        (IsaacSim + RSL-RL)                           │
├──────────────────────────────────────────────────────────────────────┤
│    AppLauncher → gym.make(task) → RslRlVecEnvWrapper                │
│         ↓                                    ↓                       │
│    IsaacSim 渲染器                   OnPolicyRunner.learn()          │
│                                      / policy(obs) → action          │
└──────────────────────────────────┬───────────────────────────────────┘
                                   │
                    ┌──────────────┴──────────────┐
                    ▼                             ▼
┌──────────────────────────────┐  ┌──────────────────────────────────┐
│    IsaacLab Env (训练侧)      │  │     MuJoCo Env (sim2sim 评估侧)  │
├──────────────────────────────┤  ├──────────────────────────────────┤
│ velocity_env_cfg.py          │  │ sim2sim_mujoco.py                │
│  ├─ Scene: 平面 + 机器人      │  │  ├─ MjModel.from_xml_path()      │
│  ├─ Commands: VC 速度指令    │  │  ├─ 手动构建 30 维 obs           │
│  ├─ Observations: 30 维      │  │  ├─ policy(obs) → action          │
│  ├─ Actions: 7 关节位置目标  │  │  ├─ q_default + scale*action     │
│  ├─ Rewards: 10 个奖励项    │  │  └─ mj_step() + 实时/加速        │
│  ├─ Terminations             │  │                                  │
│  └─ Events: reset + 随机化   │  │ sim2sim_eval.py                  │
│                              │  │  ├─ 5×5 网格 (vx×vy)             │
│ mdp/ 核心算法                 │  │  ├─ 15s/组 (warmup 3s)           │
│  ├─ virtual_chassis.py       │  │  ├─ MAE 计算                     │
│  │   SVD → VC 原点/主轴      │  │  └─ 热力图 + 轨迹汇总图          │
│  ├─ rewards.py               │  │                                  │
│  └─ commands.py              │  │                                  │
└──────────────────────────────┘  └──────────────────────────────────┘
```

---

## Virtual Chassis 算法核心

整个项目的关键创新点是 **Virtual Chassis**，其数学流程如下：

1. **收集身体位置**：取 base_link + link1~link14 共15个刚体在世界坐标系下的位置
2. **SVD分解**：对去均值后的位置矩阵做 SVD，获得正交主轴 `axes_w` (3×3)
3. **符号一致性**：与前帧对齐方向（点积判断），首次用头尾向量对齐X轴、世界Z轴对齐Z轴
4. **右手系保证**：若行列式为负则翻转Y轴
5. **速度投影**：将各刚体平均线速度/角速度投影到VC参考系 → `lin_vel_vc`, `ang_vel_z_vc`
6. **奖励计算**：在VC系中计算指令与实际速度的指数+线性惩罚

核心代码位于：
- `source/snake_project/snake_project/tasks/manager_based/velocity_tracking/mdp/virtual_chassis.py`（训练侧）
- `sim2sim/utils/virtual_chassis.py`（MuJoCo评估侧）

---

## 强化学习 MDP 参数总览

### 观测空间 (30维)

| 索引 | 观测项 | 维度 | 说明 |
|------|--------|------|------|
| 0-2 | base_ang_vel | 3 | base_link角速度（body系） |
| 3-5 | projected_gravity | 3 | 重力方向投影（body系） |
| 6-8 | velocity_commands | 3 | 目标速度指令(vx, vy, wz) |
| 9-15 | joint_pos_rel | 7 | yaw关节相对默认位置偏移 |
| 16-22 | joint_vel_rel | 7 | yaw关节速度 |
| 23-29 | last_actions | 7 | 上一步原始动作 |

### 动作空间 (7维)

- 类型：关节位置目标（Joint Position Control）
- 关节：`yaw1` ~ `yaw7`
- 缩放：`action_scale = 0.25`
- 裁剪：`[-1.57, 1.57]` rad
- 映射：`q_target = q_default + scale × action`

### 奖励函数

| 奖励项 | 权重 | 说明 |
|--------|------|------|
| VirtualChassisTrackLinVelXYExp | +5.0 | VC系线速度跟踪（指数+线性惩罚） |
| VirtualChassisTrackAngVelZExp | +1.0 | VC系角速度跟踪 |
| ang_vel_xy_l2 | -0.05 | 抑制xy角速度 |
| joint_torques_l2 | -1.0e-4 | 力矩正则化 |
| joint_acc_l2 | -2.5e-7 | 加速度正则化 |
| RawActionRatePenalty | -0.01 | 动作平滑 |
| joint_amplitude | +0.2 | 鼓励持续运动 |
| phase_propagation | +0.4 | 相邻关节交替运动 |
| motion_coordination | -0.5 | 惩罚同向运动 |
| contact_penalty | -5.0 | 自接触惩罚 |
| is_terminated | -10.0 | 终止惩罚 |

### PPO 超参数

| 参数 | 值 |
|------|-----|
| 网络结构 (actor/critic) | [512, 256, 128] |
| 激活函数 | ELU |
| 学习率 | 1.0e-3 (adaptive) |
| 每环境步数 | 24 |
| 折扣因子 γ | 0.99 |
| GAE λ | 0.95 |
| Clip参数 | 0.2 |
| 熵系数 | 0.01 |
| 学习轮数 | 5 |
| Mini-batch数 | 4 |
| 目标KL散度 | 0.01 |
| 最大梯度范数 | 1.0 |

---

## 训练与评估流程

```
 ┌─────────────────┐      ┌─────────────────┐      ┌─────────────────┐
 │    train.py     │      │    play.py      │      │  sim2sim        │
 │   (PPO 训练)    │ ────>│   (策略回放)    │ ────>│  _mujoco.py     │
 │                 │      │                 │      │  (MuJoCo 验证)  │
 │  输出:           │      │  输出:           │      │                 │
 │   - 模型 .pt    │      │   - jit 策略    │      │  输出:           │
 │   - 日志        │      │   - 视频        │      │   - 轨迹图      │
 │   - 配置 yaml   │      │   - 速度曲线    │      │   - 速度曲线    │
 └─────────────────┘      └─────────────────┘      └────────┬────────┘
                                                            │
                                                            ▼
                                                   ┌─────────────────┐
                                                   │  sim2sim        │
                                                   │  _eval.py       │
                                                   │  (批量评估)     │
                                                   │                 │
                                                   │  输出:           │
                                                   │   - 热力图      │
                                                   │   - MAE 表      │
                                                   │   - npz 数据    │
                                                   └─────────────────┘
```

---

## 可修改部分（对应作业要求）

| 文件 | 可修改内容 |
|------|-----------|
| `velocity_env_cfg.py` | 观测量(特权/噪声)、域随机化、奖励函数权重、课程学习参数、动作输出 |
| `rsl_rl_ppo_cfg.py` | PPO网络结构、学习率、clip参数、熵系数等算法超参 |

## 提交作业所需产出

1. `velocity_tracking/` 目录的 zip 包
2. 效果最佳的 jit 策略 `.pt` 文件（由 `play.py` 的 `exported/` 目录生成）
3. `eval_output/` 目录的 zip 包（由 `sim2sim_eval.py` 生成）

---

## 环境信息

| 项目 | 值 |
|------|-----|
| 训练环境 | `Snake-VelocityTracking-Flat-v0` |
| 回放环境 | `Snake-VelocityTracking-Flat-Play-v0` |
| 机器人模型 | 14DOF-DW (14自由度 + 双被动轮) |
| 控制关节 | yaw1 ~ yaw7 (7个主动关节) |
| Virtual Chassis刚体 | base_link + link1 ~ link14 (共15个) |
| 仿真时间步 | 0.005s (IsaacLab) / 0.002s (MuJoCo) |
| 控制频率 | 50Hz (decimation=4) |
| Episode长度 | 20s (训练) / 10s (回放) |
| 并行环境数 | 4096 (训练) / 1 (回放) |
| 速度指令范围 | vx: [-0.4, 0.4], vy: [-0.2, 0.2] m/s |
| Gym框架 | Gymnasium (gym.make) |
| 物理引擎 | PhysX 5 (IsaacLab) / MuJoCo (sim2sim) |
