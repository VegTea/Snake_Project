域随机化相关代码在以下两个文件：

1. 自定义随机化函数
source/snake_project/snake_project/tasks/manager_based/velocity_tracking/mdp/events.py

只包含一个活跃函数 reset_snake_state，在每次 episode 重置时调用。参数可配置：初始位置(x/y/yaw)、初始速度、关节位置扰动。

2. 事件注册配置
source/snake_project/snake_project/tasks/manager_based/velocity_tracking/velocity_env_cfg.py

SnakeVelocityEventCfg 类（约第 165-232 行）中定义了所有随机化事件：

事件|	状态|	效果
reset_robot|	活跃	|仅 x 位置抖动 (-0.2, 0.2)，其余维度固定为 0
randomize_robot_material	注释掉	摩擦系数随机化 (0.3~1.0)
randomize_link_mass	注释掉	质量缩放 (90%~110%)
randomize_link_com	注释掉	质心偏移 (±5mm)
randomize_actuator_gains	注释掉	执行器刚度/阻尼缩放 (90%~110%)

这个项目的 evaluate 分数本质上取决于 Virtual Chassis 的速度跟踪 MAE，不是 base_link，也不是姿态好不好看。sim2sim_eval.py 会扫 25 组指令：vx ∈ [-0.2,0.2]，vy ∈ [-0.1,0.1]，每组跑 15s，丢掉前 3s warm-up，然后计算 Virtual Chassis 的 vx/vy/wz 误差；核心指标是：

$$
\mathrm{planar\_MAE}
=
\mathbb{E}
\sqrt{
\left(v_x^{cmd}-v_x^{vc}\right)^2
+
\left(v_y^{cmd}-v_y^{vc}\right)^2
+
\left(v_z^{wc}\right)^2
}
$$

只修改了一个noise_std_type="log"的结果：官感上v=0.3运行比较流畅，v=0.1很别扭